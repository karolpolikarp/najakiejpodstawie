# ELI MCP Server Configuration

# Port to run the server on
PORT=8080

# API Key for authenticating requests from Supabase Edge Function
API_KEY=your-secret-api-key-here

# Optional: Cache TTL in seconds (default: 3600 = 1 hour)
CACHE_TTL=3600
