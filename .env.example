# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-public-key"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# Optional: Custom password for PasswordGate (defaults to 'lex' if not set)
# VITE_APP_PASSWORD="your-custom-password"

# Optional: Enable/disable semantic cache for AI responses (defaults to enabled)
# VITE_ENABLE_SEMANTIC_CACHE="true"

# Note: The following should be added to Supabase Edge Function Secrets
# Not in this .env file! Go to: Supabase Dashboard > Settings > Edge Functions > Secrets
# - ANTHROPIC_API_KEY: Your Anthropic API key
# - OPENAI_API_KEY: Your OpenAI API key (for embeddings)
# - ALLOWED_ORIGINS: Comma-separated list of allowed CORS origins (e.g., "https://najakiejpodstawie.pl,https://www.najakiejpodstawie.pl")
